{
  "name": "Any-To-Any-IO-Template",
  "description": "A Beam pipeline that reads from any IO and produces any IO.",
  "parameters": [
    {
      "name": "pipelineSpec",
      "label": "GCS url for the pipeline spec proto",
      "helpText": "GCS URL",
      "regexes": [
        "^gs:\\/\\/[^\\n\\r]+$"
      ],
      "paramType": "TEXT"
    }
  ]
}
