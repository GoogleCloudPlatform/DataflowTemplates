release_tracks: [ALPHA]
help_text:
  brief: |
    Export server TLS policy.
  description: |
    Export a server TLS policy.

request:
  collection: networksecurity.projects.locations.serverTlsPolicies
  api_version: v1alpha1

arguments:
  resource:
    spec: !REF googlecloudsdk.command_lib.network_security.resources:serverTlsPolicy
    help_text: |
      Name of the server TLS policy to export.
