release_tracks: [ALPHA]
help_text:
  brief: |
    Export client TLS policy.
  description: |
    Export a client TLS policy.

request:
  collection: networksecurity.projects.locations.clientTlsPolicies
  api_version: v1alpha1

arguments:
  resource:
    spec: !REF googlecloudsdk.command_lib.network_security.resources:clientTlsPolicy
    help_text: |
      Name of the client TLS policy to export.
