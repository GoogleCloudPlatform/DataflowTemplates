CREATE TABLE `source_table1` (
    `id` INT64,
    `name` STRING(MAX),
    `age` INT64,
    `member` STRING(MAX),
    `entry_added` STRING(MAX)
) PRIMARY KEY (`id`);

CREATE TABLE `source_table2` (
    `id` INT64,
    `name` STRING(MAX),
    `age` INT64,
    `member` STRING(MAX),
    `entry_added` STRING(MAX)
) PRIMARY KEY (`id`);
