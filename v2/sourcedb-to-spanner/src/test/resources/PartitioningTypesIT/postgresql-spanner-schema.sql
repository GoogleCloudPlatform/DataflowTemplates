CREATE TABLE t_float (
    id FLOAT64,
    col STRING(20)
) PRIMARY KEY (id);

CREATE TABLE t_double (
    id FLOAT64,
    col STRING(20)
) PRIMARY KEY (id);

CREATE TABLE t_numeric (
    id NUMERIC,
    col STRING(20)
) PRIMARY KEY (id);

CREATE TABLE t_decimal (
    id NUMERIC,
    col STRING(20)
) PRIMARY KEY (id);

CREATE TABLE t_date (
    id DATE,
    col STRING(20)
) PRIMARY KEY (id);
